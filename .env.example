# ==============================================================================
# Yume Protocol — Environment Variables
# ==============================================================================
# Copy this file to .env.local and fill in the values.
# NEVER commit .env.local or any file containing real secrets.

# ------------------------------------------------------------------------------
# Sui Network Configuration
# ------------------------------------------------------------------------------

# Active network: "testnet" | "mainnet"
NEXT_PUBLIC_SUI_NETWORK=testnet

# Custom RPC Endpoints (optional — defaults to public Mysten Labs endpoints)
# For production, use a dedicated node provider (e.g., Shinami, Blockberry)
# NEXT_PUBLIC_SUI_GRPC_MAINNET=https://fullnode.mainnet.sui.io:443
# NEXT_PUBLIC_SUI_GRPC_TESTNET=https://fullnode.testnet.sui.io:443

# ------------------------------------------------------------------------------
# Contract Addresses (set after deployment via `sui client publish`)
# ------------------------------------------------------------------------------

# Yume package ID on the active network
# NEXT_PUBLIC_YUME_PACKAGE_ID=0x...

# Shared object IDs for specific markets (set after market creation)
# NEXT_PUBLIC_ORDERBOOK_USDC_SUI_7D=0x...
# NEXT_PUBLIC_ORDERBOOK_USDC_SUI_30D=0x...
